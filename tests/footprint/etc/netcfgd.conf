# A configuration that uses no optional feature: no hooks, no DNS, no secrets,
# no wifi. Whatever netcfgd creates while handling this is its unconditional
# footprint, and that is what the gate pins.
global {
	on_drift = "report"
}
