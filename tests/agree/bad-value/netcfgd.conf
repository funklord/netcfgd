# A value outside an enumeration. `mac_policy = "random"` is the mistake
# `tool/example_gate.py` records finding in the shipped example file, which is
# why it is the one used here.
device wlan0 {
	wifi {
		mac_policy = "random"
	}
}
