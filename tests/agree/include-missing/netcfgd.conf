# An include that names a file which is not there. **The one case in this
# corpus where neither program names a position**: the loader refuses before
# anything is parsed, so there is no line to point at -- in either. The gate
# checks that they agree about *that* rather than about a sentence.
include "nowhere/at/all.conf"
