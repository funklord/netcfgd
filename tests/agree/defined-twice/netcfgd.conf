# The same block twice. Both programs point at the second one, because that is
# the line somebody has to delete or write `override` on.
interface eth0 {
	config = "dhcp"
}
interface eth0 {
	config = "10.0.0.1/24"
}
