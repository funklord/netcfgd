global {
	dns { mode = "write_resolv_conf" }
}

device eth0 {
	mtu = 1492
	qdisc {
		kind      = "cake"
		bandwidth = "100mbit"
	}
}

device br0 {
	bridge {
		stp = true
	}
}

interface eth0 {
	preference = 100
	config     = "dhcp"
	dns {
		servers = ["10.0.0.1", "2001:db8::1"]
	}
}

interface br0 {
	config = "10.9.0.2/24"
}

network "café-guest" {
	wifi { psk = "@secret:cafe" }
}
