# Per-binary size limits in bytes, checked by `make size`.
#
# These are NOT section 10.2's tier budget. That document sets embedded at 1 MB
# and calls it a "budget to validate, not a measurement". (It also set nano at
# 400 KB; decision 0021 dropped that tier.)
# What is built today is the full tier: every feature compiled in, and two
# separate static binaries that each link most of the workspace. Meeting the
# embedded budget is M5's job, through the build profiles that compile the DSL
# compiler, the client and the adapters out -- and probably through making
# `ncfg` and `netcfgd` one multi-call binary, since duplicating the shared
# crates is most of what is being paid for here.
#
# Measured as **total installed size**, not per binary. What a 16 MB router
# cares about is how much flash the install takes; a per-binary limit would call
# merging two binaries a regression when it saves a megabyte of flash. The
# per-binary numbers are still printed, because "which one grew?" is the next
# question -- but only the total is a gate.
#
# What this file does instead is ratchet. A limit is the last measured size
# plus a small tolerance, so ordinary growth fails the build and raising a
# number is a deliberate, reviewable edit rather than something that happens
# quietly. Constraint 8's point is not the specific figure, it is that nobody
# gets to drift upward without saying so.
#
# Tolerance covers compiler-version noise; a real feature will exceed it.
tolerance_percent 3

# M3, the supplicant and the wifi config syntax. netcfgd grew 86 KB and ncfg
# 37 KB across two steps: the control-socket client and the wireless request
# handlers, then the compiler learning `network` and `device.wifi` blocks --
# which both binaries carry, since `ncfg` compiles the config itself. Plus
# serde's generated code for the new protocol types.
#
# No new dependency in any of it. The whole increase is code that was written
# rather than pulled in, which is what constraint 3 was protecting.
#
# Recorded at the measured size rather than left to ride the tolerance: the 3%
# is for compiler-version noise, and spending it on a real feature means the
# next real feature fails the gate for the wrong reason.
#
# Then wired 802.1X: netcfgd-apply gained the supplicant client, because
# starting a supplicant and filling it is one action rather than two. ncfg took
# the larger share (+45 KB against netcfgd's +12 KB) since it links the
# executor for `--oneshot` and did not already carry the supplicant -- `ncfg
# apply` really can bring up an 802.1X port, so the code has to be there.
#
# Then the M4 model gaps: policy routing rules, access points, ethtool
# settings, IPv6 tokens and MAC randomization -- types, serde and compiler
# lowering for five features of which one is implemented. netcfgd +61 KB, ncfg
# +29 KB, and most of it is serde's generated code for the new types rather
# than logic. That is the cost of the freeze: three of these produce no
# behaviour today and are carried anyway, because adding them after M4 is a
# major version bump.
#
# Then M4 link types: bond, vxlan and veth creation, per-kind attributes for
# all of them, and the member-list expansion. +12 KB each, which is what
# actual netlink encoding costs when the types already exist.
#
# Then generic netlink and WireGuard: the family resolver, the wg encoder and
# reader, the base64 key type, and the compiler lowering. +25 KB and +29 KB.
# The genl half is small; most of this is WireGuard, and both binaries carry it
# because `ncfg apply --oneshot` configures a tunnel as readily as the daemon.
#
# Then prefix delegation: the arithmetic, the observed delegation list, the
# DHCPv6 client hook and the planner resolution. +20 KB and +16 KB.
#
# Then the five scope-capable DNS backends: openresolv, resolved, dnsmasq,
# unbound and exec. +12 KB each -- the renderers are text and the delivery is
# a process spawn or a file write, so none of them is large.
#
# Then the pre-freeze format audit's findings: VRF, macvlan, the tunnel family
# (gre/gretap/ip6gre/ipip/sit/ip6tnl/geneve), tun/tap as schema-only, a
# spelling for parameterless kinds, and four more bridge parameters. +16 KB and
# +37 KB -- the tunnel encoding is three attribute families that disagree about
# numbering, which is most of it.
#
# Then PPPoE: the compiler, the planner treating a session as a prerequisite
# rather than a link, and the executor writing pppd's options file. +12 KB each.
# The binaries the gate sums. A name with no number is measured, not limited.
#
# One name now, not two. `ncfg` is a symlink to `netcfgd` -- see the multi-call
# note below -- so summing both would count the same file twice.
netcfgd

# 1 MB was design section 10.2's embedded budget. Superseded by 0024, which
# measured what closing the gap would take and found no route that does not
# give up something this project will not give up. The number below is now a
# measurement that ratchets, like every other number in this file.
#
# +37 KB for bridge VLANs: a second netlink dump under AF_BRIDGE, the setter,
# the range expansion, and the planner's per-port reconciliation.
#
# +8 KB for carrier-based switching: the preference field, metric resolution,
# and the DHCP client finally being told the metric the model always carried.
#
# +12 KB for detecting another daemon on the same interface.
#
# +72 KB for NAT and forwarding, which is the largest single step since M3 and
# was predicted: decision 0022 said "it costs size, and the budget is already
# over". It buys a whole second netlink protocol -- `NETLINK_NETFILTER`, its
# batched transaction framing, an nftables expression encoder and the reader
# that parses rules back -- plus the forwarding sysctls, two planner passes and
# two executor arms.
#
# This was recorded as "the one to reach for first when the install has to
# shrink". Measured at 0024 and wrong: the whole netlink crate -- nftables,
# qdisc, filters, WireGuard, generic netlink and all of rtnetlink -- is 11,904
# bytes of symbols. The 72 KB was model fields, their derived serde code, the
# planner passes and the format strings, none of which an optional backend
# removes, because a frozen document format has to parse `nat = true` on a
# build that cannot do it.
# +40 KB for root qdiscs: `struct tcmsg`, the set/reset/dump messages, the
# `cake` rate encoding, the scheduler enum and its config lowering, plus a
# planner pass and two executor arms. A third of what NAT cost, because this is
# rtnetlink on the socket netcfgd already holds rather than a second protocol
# -- which is the measured version of what decision 0023 predicted.
# +56 KB for ingress shaping: the `ifb` link kind, the ingress hook, a
# `matchall` classifier carrying a `mirred` redirect, the filter dump that
# reads it back, plus the compiler expansion that turns one config key into a
# synthesised device and a redirect. More than the qdisc work itself, because
# the filter is three levels of nesting to write and to parse.
# +8 KB for read-only-root support: a second config layer, `ncfg reset`, and
# the file enumeration they share. All of it in ncfg -- the daemon did not
# grow, because reading two directories instead of one is the same code path.
# -1149376 for merging the two binaries into one, which is 40% of the install
# and the single largest change this file has ever recorded.
#
# It was measured before it was done, because the last size premise on this
# project was inverted when checked (decision 0021). `nm` on the two release
# binaries put 775 KB of symbols in both of them -- the model, the compiler,
# the planner, the executor and the netlink layer, which is nearly everything
# either program is. Shipping that twice was a quarter of the install spent on
# identical machine code, on the class of device with single-digit megabytes of
# flash free. The realised saving is larger than the symbol figure because a
# second ELF also carries its own headers, relocations and unwind tables.
#
# So they are one file with two names, as busybox and util-linux do it. The
# binary is `netcfgd` and `ncfg` is a symlink; each still has its own argument
# parsing, usage text and exit codes, because they are still two programs.
# +28 KB for `ncfg tui`: four panes, and the ncurses layer in the audited
# crate. Small because it reads answers as raw JSON rather than pulling in the
# derived deserialiser for the document -- the same reason the client's
# `Answer` type is narrow, and worth about ten times what the TUI itself costs.
#
# The figure is unchanged from the hand-rolled ANSI version this replaced,
# which is not a coincidence about the code: 28672 is exactly seven pages, the
# linker pads to page boundaries, and so this gate cannot see a difference
# smaller than 4 KB. `--no-default-features` drops the TUI and links no ncurses,
# returning the binary to 1743384 -- the size it was before any TUI existed.
# +20 KB for policy routing rules: the fib_rule encoder and dump decoder, the
# observed type, a reconciliation pass and two executor arms. This closes one
# of the four features the M4 freeze put in the schema with nothing behind
# them, so it removes a plan warning as well as adding a feature.
# +4 KB for `ipv6_token`: one setter through IFLA_AF_SPEC, the token read out
# of the link dump, a planner pass and an executor arm. Small because the dump
# already carried it -- reading it needed no second request.
# +16 KB for the ethtool offloads: the generic-netlink family, the named
# bitset encoder and decoder, a per-link query in the observation and a planner
# pass. The family resolution itself was already there, built for WireGuard --
# which is what made this reachable at all, since decision 0016 had put it
# behind either that or an ioctl the unsafe policy forbids.
# +32 KB for access points: the hostapd configuration renderer, the secret
# resolution and file write around it, a planner pass, three warnings and two
# executor arms. The largest of the four frozen-but-inert features to close,
# and the only one that is not netlink -- no new dependency, but a whole
# backend crate of text generation plus the refusals that keep it honest.
#
# Recorded rather than left to ride the 3% tolerance, for the reason at the top
# of this file: the tolerance is for compiler-version noise, and spending it on
# a feature makes the next feature fail the gate for the wrong reason.
# +8 KB for honouring `managed = false`: one check at the planner's action
# choke point, the DNS scope exception beside it, and the warning that names
# the credentials walking away leaves behind. Small because the enforcement is
# one place rather than eleven -- which was the point of putting it there.
# +4 KB for `on_unmanage = "clear"`: the policy enum and its serde, the
# compiler key, the clearing set, and the document filter that lets four
# teardown passes implement it without knowing it exists.
# +48 KB for showing who is connected: the STA-FIRST/STA-NEXT walk and its
# parser, two protocol types with their serde, the daemon handler, an `ncfg
# wifi clients` renderer and a fifth TUI pane. The daemon also links
# netcfgd-hostapd for the first time -- netcfgd-apply already did, so the code
# was in the binary, but the dependency edge is new.
#
# Most of this is serde's generated code for StationReport and StationEntry
# again, which is the same finding as every previous entry in this file: the
# types cost more than the logic.
# +12 KB for converging an access point's ACL over the control socket: the
# DENY_ACL/ACCEPT_ACL client and its SHOW parser, two observed types with their
# serde, a planner pass with its restart path, two ops and two executor arms.
# `netcfgd-observe` links netcfgd-hostapd for the first time, which is a new
# dependency edge and no new code in the binary -- netcfgd-apply already linked
# it, and `ncfg` links both.
#
# Cheaper than the station list at +48 KB despite doing more, and for the
# reason this file keeps finding: that one added two protocol types that cross
# the wire, and this one adds no wire types at all. The lists are read and
# converged where they are used.
# +12 KB for refusing to strand a key nobody can revoke: the WireGuard key
# read in the observation, an observed bool, the `Stranded` type with its serde,
# a planner pass, a CLI flag with its own exit code, and the proto field that
# carries the consent to the daemon.
#
# The same shape as every entry above it -- the rule itself is thirty lines and
# most of this is the type crossing two boundaries. What it is not is a second
# netlink protocol: `wg::get_device` was written for M4 and had never been
# called outside a test, so reading a key back cost nothing new.
# +8 KB for the modem reporting contract: the observed type with its serde, the
# key=value reader, and the status rendering that says a report was seen and not
# applied. No protocol, no dependency and no new netlink -- netcfgd's half of
# this feature is reading a file, which is the whole point of decisions 0044 and
# 0045.
# +4 KB for the addressing source that consumes the report: an enum variant
# with its serde, a compiler keyword, a planner arm and the teardown arm that
# makes withdrawal work. Four kilobytes because the address path was already
# there -- this source adds a place the value comes from, not a way to install
# one.
# +4 KB for the default route a modem report implies: the route synthesis, the
# shared list the forward pass and the teardown both read, and the tests. Small
# because the routes are *synthesised into the existing type* and go through
# the ordinary route path -- carrier check, metric, ordering rule 4, teardown --
# rather than getting a planner of their own to keep in step.
# +8 KB for the nameservers a modem report contributes: the shared scope
# function both the planner and the executor now call, the merge with what the
# document wrote, and the mode inheritance that closes an older defect.
#
# Bigger than the route for a reason worth recording: the scopes are *owned*
# rather than borrowed, because one of them is synthesised from an observation
# and there is nothing to borrow it from. Cloning a DnsPolicy per interface per
# plan is what that costs, and it is what having one implementation instead of
# two is worth.
# +20 KB for the OpenVPN tunnel: the interface kind and backend kind with their
# serde, the compiler block, the planner arms, and a backend crate carrying a
# unix *stream* management client -- which is new code rather than a reuse,
# because wpa_ctrl is a datagram protocol and this one is not.
#
# The largest single step since the station list, and almost none of it is the
# integration: netcfgd does not generate OpenVPN's configuration (decision
# 0046), so there is no renderer here at all. What it buys is a lifecycle --
# start, stop through the daemon's own socket, and quote what it said.
# +4 KB for the tunnel's credentials: two optional model fields with their
# serde, the compiler keys and the refusal that pairs them, and the 0600
# two-line file `--auth-user-pass` reads. Small because the secret machinery
# was already there -- the resolver, the provider and the `SecretRef` type all
# predate this, and what was added is one more place that calls them.
# +12 KB for the routes a report can name and the tunnel that reports them.
# Three pieces, none of them large on its own: a `ReportedRoute` in the model
# with its serde, the parsing and planning of a route line, and the shell
# script netcfgd generates for openvpn's --route-up -- which is a format string
# in the binary and is most of this step.
#
# It also carries the rename of the reporting contract, which cost nothing: the
# same code under a different word.
# +4 KB for a DSL line's resolvers: two generated scripts instead of none, and
# the options-file lines that point pppd at them. Small because everything else
# it needed already existed -- the reporting contract, its reader, and the rule
# that decides whether a reported nameserver is delivered.
# +4 KB for the DHCPv6 client decision: the refusal message that names why
# dhcpcd cannot report a delegated prefix, and the per-interface lookup that
# decides whether a prefix was asked for at all. Mostly the message, which is
# the point of it -- a lease that goes nowhere is the failure it prevents.
# +4 KB for the request half of prefix delegation: three modifier words in the
# compiler with their diagnostics, the per-interface lookup that carries the
# request to odhcp6c, and the refusal of the modifiers the keyword sources used
# to drop silently. The DSL half is most of it, which is where it should be.
# +24 KB for router advertisement, which is the largest step since the OpenVPN
# tunnel and for the same reason: a new backend crate, a new config block in
# the compiler with its diagnostics, a planner arm with its own waiting rule,
# and the executor path that resolves a prefix reference at the last moment.
#
# The renderer is a small part of it. What costs is that `advertise` is the
# fourth thing in the document with a backend behind it, and each of those
# brings its own start, stop, ownership check and error vocabulary.
# +8 KB for noticing that a delegation was renumbered: the observed field that
# says what a running radvd was last given, the planner comparison against what
# the delegation now implies, and the reload that rewrites and signals. Small
# because the expensive half -- a backend with a lifecycle -- was already paid
# for by the advertisement itself.
# +8 KB for asking the same question of an access point: the observed identity
# read back from the file netcfgd wrote, the comparison in the planner, and the
# restart with the warning about what it costs. The same shape as the
# advertisement one commit earlier, which is why it is half the price.
# +8 KB for `ncfg explain` catching up with what the reconciler learned this
# session: the indirection behind a delegated address, and the three facts about
# a running backend that the file netcfgd wrote is the only account of. The
# command whose whole job is "why is it like this" was the last thing that could
# not say why an access point restarted.
# +4 KB for the TUI catching up with the same two things: what was reported and
# not applied, and a backend that is about to be restarted. It is the pane an
# operator watches while a modem is not working, and it was the one view where
# "the bearer is up" and "netcfgd acted on it" looked the same.
# +16 KB for a WireGuard device netcfgd can see and correct: the observation
# carries what the kernel holds -- listen port, firewall mark, public key and
# the peer list -- and the planner compares it against the document. What it
# bought is a revocation that happens: deleting a peer from the config used to
# plan nothing at all, so the peer kept its tunnel while `ncfg apply` said
# there was nothing to do. Half of the cost is the observed model growing two
# structs; the rest is the comparison and the two ops that were declared in the
# taxonomy and had never been emitted.
# +8 KB for a bridge netcfgd can see and correct: the link dump reads back the
# INFO_DATA nest a bridge reports about itself, and the planner compares it
# against the document. What it bought is an edited `stp` or `forward_delay`
# doing something -- before this they were sent inside `link.create` and never
# again, and a bridge's name encodes nothing, so nothing anywhere said so.
# +8 KB for the same question asked of a bond: the dump reads its mode and
# monitoring interval back, and the planner compares them. What it bought is an
# edited miimon being applied -- and an edited *mode* being explained instead,
# because the kernel takes one only on a bond with no members and rejects the
# whole message otherwise, interval included.
# +16 KB for the rest of the kinds 0057 named: a macvlan's mode, a tunnel's
# endpoints and a VXLAN's. Three decoders rather than one, because GRE, the ip
# tunnels and geneve number their attributes independently and reading one with
# another's constants is how a VXLAN comes to report a forward delay. What it
# bought is an edited endpoint moving at all, and -- for the three edits the
# kernel refuses -- a sentence saying what to do instead of an apply that fails
# and is planned again forever.
# +12 KB for the last shape on that list: what the kernel accepts and ignores.
# A VLAN's id and tag protocol are set at creation and `vlan_changelink` drops
# them afterwards, so the only way to apply an edited id is to delete the
# interface and make it again -- and an interface that existed as an entirely
# different kind was not noticed at all. What it bought is an edited id being
# applied with everything on the interface put back, and the safety property
# that goes with it: netcfgd will not throw away a link it did not create.
# +8 KB for a parent that reaches the kernel and is then compared. A VXLAN's
# underlay and a tunnel's were being sent as the outer `IFLA_LINK`, which those
# two kinds ignore -- so `parent = "base0"` produced a device with no underlay at
# all and nothing said so. Both now go inside their own nest, the observation
# reads one back, and a moved parent is corrected: in place for the two kinds the
# kernel moves, and by remaking the interface for the two it does not.
# +8 KB for four config keys that compiled and did nothing. Two are implemented:
# `slaac privacy prefer_temporary` writes `use_tempaddr` per interface, which is
# what a laptop on other people's networks wants, and `hostname = "name"` sets the
# running hostname. Two are reported instead, with the reason: `hostname = "dhcp"`
# needs a lease netcfgd never sees, and `portal_check` would need netcfgd to fetch
# a hard-coded URL. The same pass caught nine hook phases that are recognised,
# materialised, hashed and never run -- which read exactly like a working feature.
# +12 KB for rfkill: the observation reads whether an interface's radio is
# switched off, `ncfg status` and `ncfg explain` say so, and a plan names the
# remedy for the switch that did it. What it bought is the answer to "why will
# this not associate" on a laptop, where the switch is one keystroke away at all
# times and a blocked radio looks exactly like a network that is not there.
# +8 KB for the two hook phases an operator reaches for first. `down` and
# `post_down` bracket the action that takes an interface away, and `lease` fires on
# an address netcfgd did not install -- which is the only way netcfgd can know a
# lease arrived, since it never sees DHCP. Most of the cost is the `/run` record
# that makes the lease hook fire once per lease instead of once per reconcile.
# +8 KB for a lease's nameservers, which reached nothing before: both clients now
# run a netcfgd-generated script that writes them into the interface report, and
# 0049's existing gate delivers them to an interface that asked with `dns { }`. The
# dhcpcd half also ends a fight nobody had noticed -- its own `20-resolv.conf` hook
# was writing the file netcfgd's DNS mode owns. And a delivery with no servers in it
# now says so instead of silently writing a resolver file that resolves nothing.
# +4 KB for a lease's search suffixes, which are the weaker half of what a server
# pushes: what to append to a bare name, never which resolver answers. Same gate as
# the servers -- a party already answering every query gains nothing by appending a
# suffix -- and all three writers report it, so one contract means one thing.
# +20 KB for `ncfg wifi add`, which is the last thing on a laptop that needed an
# editor and root. It writes one `network` block into conf.d, stores the passphrase
# in secrets/ at 0600 with the mode set by the open, and compiles the result back
# through the daemon's own loader -- removing what it wrote if that does not hold.
# Most of the cost is the diagnostics: a refusal that names the file, the id and
# the fix is worth more here than anywhere else, because the operator running this
# has no network to look anything up with.
# +4 KB for `accept_ra`, which is one sysctl and the answer to a question that
# cost an hour the first time it was asked here: `accept_ra` defaults to "accept
# unless this interface forwards", so `config = "slaac"` on a router's WAN -- or in
# a container, which usually forwards -- obtained no address at all while the apply
# reported success. Most of the cost is the reading, which has to combine the
# sysctl with the interface's IPv6 forwarding before either half means anything.
# +16 KB for `ncfg secret set`, which is the last credential a laptop or a router
# needed an editor and a `chmod` for -- a WireGuard key, a DSL password, an
# 802.1X certificate key. The value is never an argument (`ps` shows one to every
# user) and never echoed, and the file is 0600 from the moment it exists rather
# than after a chmod. Most of the cost is the report: which blocks refer to the
# name, which is what turns a typo into a sentence instead of a backend failing
# with "no such secret" an hour later.
# +4 KB for noticing a daemon that died on its own and restarting it -- and for
# the counter that stops that becoming a start storm. A tunnel killed by the OOM
# killer used to leave netcfgd reporting a converged network with nothing behind
# it; now the observation checks the pid of every backend netcfgd holds one for,
# and the planner starts what has gone. The five-start limit is the other half:
# without it, an interface set to `reconcile` produced 181 starts in twelve
# seconds for a daemon that lived half a second -- measured, on the change that
# introduced it.
# +52 KB across the twenty-eight commits since the entry above, measured by
# building the release binary at six points along that range rather than
# attributed by guess:
#
#   d2db524  2210328   the last figure this file recorded
#   1a6f5dc  2210328   +0   noticing a dead supplicant by its pid file
#   8e561f8  2218520   +8   the drift hook, `ncfg reload`, one name per op
#   62204bd  2234904   +16  a wedged daemon's `answering`, a v6 lease's
#                           nameservers, enterprise networks from the CLI,
#                           and a station roaming to the loudest AP
#   09b4372  2251288   +16  naming a network by its access points, the roam
#                           hook and its watcher, control tiers, rfkill events
#   871ca25  2263576   +12  the commit-confirm window a document asks for,
#                           and the captive-portal probe
#   HEAD     2263576   +0   everything since
#
# The last row is the one worth reading. Five changes land in it -- the
# `pre_down` teardown ordering, the dangling-dependency fix, asking a
# supplicant whether it answers, two package formats, and the zombie check --
# and the gate cannot see any of them. The linker pads to 4 KB pages, which
# this file already knew from the TUI entry; a step smaller than a page is
# invisible here, and several small steps can hide inside one until they
# cross.
#
# Which is how this drifted at all. It was never one feature that overspent:
# it was 2.41% accumulated four kilobytes at a time, against a 3% tolerance
# that exists for compiler noise. Thirteen kilobytes of that tolerance were
# left, so the next real feature would have failed this gate for exactly the
# wrong reason -- which is the sentence at the top of this file, happening.
#
# Recorded at the measured size, as every entry above it is.
#
# +4 KB on top of that for `NCFG_OPENVPN`: three lines in the backend's binary
# lookup, which is a page. It is here rather than folded into the row above
# because it is not a feature -- it is what it cost to make `openvpn.sh` test
# netcfgd on a machine that has openvpn installed (0101).
#
# +68 KB across the one hundred and twenty-four commits since the entry above,
# measured by building the release binary at thirteen points along that range
# in a separate worktree rather than attributed by guess:
#
#   e98f74f  2267672   the figure this file last recorded
#   a5eac3a  2267672   +0
#   973c54d  2267672   +0
#   3f1e1dc  2267672   +0
#   cbe6afe  2267672   +0
#   77d3f5e  2267672   +0
#   d55f538  2267672   +0
#   d972479  2279960   +12  a network added over the socket
#   53a306b  2312728   +32  the reserved group and the typed control policy
#   6b2e264  2312728   +0
#   d40eabe  2329112   +16  an uplink's routes withheld on a probe verdict
#   4a0d120  2337304   +8   the client's privileged half, bounded and tested
#   6695813  2337304   +0
#   6e93a6a  2337304   +0
#   HEAD     2337304   +0   everything since, the two /run concurrency fixes
#                           (0121, 0122) included
#
# **Nothing in `gui/` can be in this number**, which is what makes the four
# steps attributable at all. Eleven of these commits are the Qt client, and it
# is a separate build that links none of this -- so each step falls on the one
# or two Rust commits inside its window, and the windows were chosen small
# enough to leave no ambiguity about which.
#
# The four, in order:
#
#   +12 KB, `netcfgd-daemon: add a network from the socket`. A typed request
#   with its fields, its serde, the daemon handler and the authorisation. The
#   same finding as nearly every entry in this file: a type that crosses the
#   wire costs more than the logic behind it.
#
#   +32 KB, the reserved `netcfgd` group and the typed control policy, plus
#   editing the global block in place. The largest of the four and the least
#   surprising: it is a second configuration writer, with the diagnostics that
#   make an unprivileged refusal explain itself rather than being a silent no.
#
#   +16 KB, withholding an uplink's routes when its probe says the network is
#   not usable -- the planner pass, the verdict held down in the daemon, and
#   `explain` naming it. A feature with a decision record (0119) rather than
#   drift.
#
#   +8 KB, bounding and testing the half of the client that runs as root.
#
# So this one is *not* the shape the entry above it describes. That was 2.41%
# accumulated four kilobytes at a time with nothing to point at; this is four
# named features, each of which would have been recorded here had anybody run
# `make size` between them. That nobody did is the finding worth carrying:
# `make check` has been red on this gate for some sixty commits, and a gate
# that is red for long enough stops being read at all.
#
# Recorded at the measured size, as every entry above it is.
#
# 2026-08-20: 2337304 -> 2406936, +69632 bytes, for 0127's collapse and what
# it needed.
#
#   +~20 KB, `netcfgd-compile`'s `privilege` module and the classification it
#   carries. The daemon now parses text a client sent and answers what each
#   production grants before writing it, which is what makes an open `admin`
#   tier survivable rather than equivalent to handing out root.
#
#   +~18 KB, four socket requests and their handlers -- `config_put`,
#   `secret_put`, `config_delete`, `secret_delete` -- plus the drop-in writer
#   in `netcfgd-host`, which compiles the whole directory back and restores
#   what was there when the result would not compile.
#
#   +~16 KB, `ncfg config put|rm`. The client half, without which the four
#   requests above were reachable only by a program somebody wrote themselves.
#
#   +~16 KB, the local/remote split (0128): a second origin, a second policy
#   in the model, and a second socket bound when a remote policy opens
#   anything.
#
# Recorded because the gate had 487 bytes of tolerance left, which is the
# state this file's own header warns about: the 3% is for compiler-version
# noise, and a feature that spends it makes the *next* feature fail for a
# reason that is not its own. Ratcheting now is what stops that, and it is
# what the entry above this one was written after failing to do for sixty
# commits.
#   +~74 KB across M8's wireless work, ratcheted once here rather than six
#   times: the radio switch (`radios` and `radio_set`, and the `device` plus
#   `interface` blocks activation writes), the enterprise arm of `wifi_add`,
#   fast transition beside every base key management mode, `enterprise` on a
#   scan entry and `wireless` on a link, the panic hook, and the plan carrying
#   contention so that a client other than `ncfg` can say why a contended
#   radio behaves oddly.
#
#   That last one is the smallest and the reason the gate fired: 1520 bytes
#   over a ceiling with the 3% already spent. Which is the header's point --
#   the tolerance is for compiler noise, and a feature that eats it makes the
#   next one fail for a reason that is not its own.
total 2480664
